import React from 'react';

export default function TimetableEditor() {
  return (
    <div className="card">
      <h2 className="card-title">Manage Timetable</h2>
      <p style={{ color: 'var(--text-muted)' }}>Create and modify time slots for classes.</p>
    </div>
  );
}
